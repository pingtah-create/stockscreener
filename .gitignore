# Cache & data (don't commit — too large, refreshes automatically)
cache/
static/cache/

# Auth state (users.json, secret.key) — never commit
auth/

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# Server logs
server.log
*.log

# OS
.DS_Store
Thumbs.db

.vercel
test_server.log

# Claude Code / skills artifacts — local dev only
.agents/
.claude/skills/
skills-lock.json

# Hyperframe video composition source — re-renderable, keep only the MP4
landing/video/
