# Cache & data (don't commit — too large, refreshes automatically)
cache/
static/cache/

# Auth state (users.json, secret.key) — never commit
auth/

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# Server logs
server.log
*.log

# OS
.DS_Store
Thumbs.db

.vercel
test_server.log

# Claude Code skill artifacts (local tooling, not project source)
.agents/
.claude/skills/
skills-lock.json
landing/
